package com.bomu.game.socket.handler;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class Action {
    protected final Logger logger = LogManager.getLogger(getClass());

}
